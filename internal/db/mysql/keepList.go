// Code generated by SQLBoiler 4.15.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package mysql

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// KeepList is an object representing the database table.
type KeepList struct {
	KeepId   int64       `boil:"keepId" json:"keepId" toml:"keepId" yaml:"keepId"`
	MemberId int64       `boil:"memberId" json:"memberId" toml:"memberId" yaml:"memberId"`
	KeepName null.String `boil:"keep_name" json:"keep_name,omitempty" toml:"keep_name" yaml:"keep_name,omitempty"`

	R *keepListR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L keepListL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var KeepListColumns = struct {
	KeepId   string
	MemberId string
	KeepName string
}{
	KeepId:   "keepId",
	MemberId: "memberId",
	KeepName: "keep_name",
}

var KeepListTableColumns = struct {
	KeepId   string
	MemberId string
	KeepName string
}{
	KeepId:   "keepList.keepId",
	MemberId: "keepList.memberId",
	KeepName: "keepList.keep_name",
}

// Generated where

var KeepListWhere = struct {
	KeepId   whereHelperint64
	MemberId whereHelperint64
	KeepName whereHelpernull_String
}{
	KeepId:   whereHelperint64{field: "`keepList`.`keepId`"},
	MemberId: whereHelperint64{field: "`keepList`.`memberId`"},
	KeepName: whereHelpernull_String{field: "`keepList`.`keep_name`"},
}

// KeepListRels is where relationship names are stored.
var KeepListRels = struct {
	MemberIdMember  string
	KeepIdKeepSongs string
}{
	MemberIdMember:  "MemberIdMember",
	KeepIdKeepSongs: "KeepIdKeepSongs",
}

// keepListR is where relationships are stored.
type keepListR struct {
	MemberIdMember  *Member       `boil:"MemberIdMember" json:"MemberIdMember" toml:"MemberIdMember" yaml:"MemberIdMember"`
	KeepIdKeepSongs KeepSongSlice `boil:"KeepIdKeepSongs" json:"KeepIdKeepSongs" toml:"KeepIdKeepSongs" yaml:"KeepIdKeepSongs"`
}

// NewStruct creates a new relationship struct
func (*keepListR) NewStruct() *keepListR {
	return &keepListR{}
}

func (r *keepListR) GetMemberIdMember() *Member {
	if r == nil {
		return nil
	}
	return r.MemberIdMember
}

func (r *keepListR) GetKeepIdKeepSongs() KeepSongSlice {
	if r == nil {
		return nil
	}
	return r.KeepIdKeepSongs
}

// keepListL is where Load methods for each relationship are stored.
type keepListL struct{}

var (
	keepListAllColumns            = []string{"keepId", "memberId", "keep_name"}
	keepListColumnsWithoutDefault = []string{"memberId", "keep_name"}
	keepListColumnsWithDefault    = []string{"keepId"}
	keepListPrimaryKeyColumns     = []string{"keepId"}
	keepListGeneratedColumns      = []string{}
)

type (
	// KeepListSlice is an alias for a slice of pointers to KeepList.
	// This should almost always be used instead of []KeepList.
	KeepListSlice []*KeepList
	// KeepListHook is the signature for custom KeepList hook methods
	KeepListHook func(context.Context, boil.ContextExecutor, *KeepList) error

	keepListQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	keepListType                 = reflect.TypeOf(&KeepList{})
	keepListMapping              = queries.MakeStructMapping(keepListType)
	keepListPrimaryKeyMapping, _ = queries.BindMapping(keepListType, keepListMapping, keepListPrimaryKeyColumns)
	keepListInsertCacheMut       sync.RWMutex
	keepListInsertCache          = make(map[string]insertCache)
	keepListUpdateCacheMut       sync.RWMutex
	keepListUpdateCache          = make(map[string]updateCache)
	keepListUpsertCacheMut       sync.RWMutex
	keepListUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var keepListAfterSelectHooks []KeepListHook

var keepListBeforeInsertHooks []KeepListHook
var keepListAfterInsertHooks []KeepListHook

var keepListBeforeUpdateHooks []KeepListHook
var keepListAfterUpdateHooks []KeepListHook

var keepListBeforeDeleteHooks []KeepListHook
var keepListAfterDeleteHooks []KeepListHook

var keepListBeforeUpsertHooks []KeepListHook
var keepListAfterUpsertHooks []KeepListHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *KeepList) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range keepListAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *KeepList) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range keepListBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *KeepList) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range keepListAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *KeepList) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range keepListBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *KeepList) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range keepListAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *KeepList) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range keepListBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *KeepList) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range keepListAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *KeepList) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range keepListBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *KeepList) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range keepListAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddKeepListHook registers your hook function for all future operations.
func AddKeepListHook(hookPoint boil.HookPoint, keepListHook KeepListHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		keepListAfterSelectHooks = append(keepListAfterSelectHooks, keepListHook)
	case boil.BeforeInsertHook:
		keepListBeforeInsertHooks = append(keepListBeforeInsertHooks, keepListHook)
	case boil.AfterInsertHook:
		keepListAfterInsertHooks = append(keepListAfterInsertHooks, keepListHook)
	case boil.BeforeUpdateHook:
		keepListBeforeUpdateHooks = append(keepListBeforeUpdateHooks, keepListHook)
	case boil.AfterUpdateHook:
		keepListAfterUpdateHooks = append(keepListAfterUpdateHooks, keepListHook)
	case boil.BeforeDeleteHook:
		keepListBeforeDeleteHooks = append(keepListBeforeDeleteHooks, keepListHook)
	case boil.AfterDeleteHook:
		keepListAfterDeleteHooks = append(keepListAfterDeleteHooks, keepListHook)
	case boil.BeforeUpsertHook:
		keepListBeforeUpsertHooks = append(keepListBeforeUpsertHooks, keepListHook)
	case boil.AfterUpsertHook:
		keepListAfterUpsertHooks = append(keepListAfterUpsertHooks, keepListHook)
	}
}

// One returns a single keepList record from the query.
func (q keepListQuery) One(ctx context.Context, exec boil.ContextExecutor) (*KeepList, error) {
	o := &KeepList{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "mysql: failed to execute a one query for keepList")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all KeepList records from the query.
func (q keepListQuery) All(ctx context.Context, exec boil.ContextExecutor) (KeepListSlice, error) {
	var o []*KeepList

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "mysql: failed to assign all query results to KeepList slice")
	}

	if len(keepListAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all KeepList records in the query.
func (q keepListQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "mysql: failed to count keepList rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q keepListQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "mysql: failed to check if keepList exists")
	}

	return count > 0, nil
}

// MemberIdMember pointed to by the foreign key.
func (o *KeepList) MemberIdMember(mods ...qm.QueryMod) memberQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`id` = ?", o.MemberId),
	}

	queryMods = append(queryMods, mods...)

	return Members(queryMods...)
}

// KeepIdKeepSongs retrieves all the keepSong's KeepSongs with an executor via keepId column.
func (o *KeepList) KeepIdKeepSongs(mods ...qm.QueryMod) keepSongQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`keepSong`.`keepId`=?", o.KeepId),
	)

	return KeepSongs(queryMods...)
}

// LoadMemberIdMember allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (keepListL) LoadMemberIdMember(ctx context.Context, e boil.ContextExecutor, singular bool, maybeKeepList interface{}, mods queries.Applicator) error {
	var slice []*KeepList
	var object *KeepList

	if singular {
		var ok bool
		object, ok = maybeKeepList.(*KeepList)
		if !ok {
			object = new(KeepList)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeKeepList)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeKeepList))
			}
		}
	} else {
		s, ok := maybeKeepList.(*[]*KeepList)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeKeepList)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeKeepList))
			}
		}
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &keepListR{}
		}
		args = append(args, object.MemberId)

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &keepListR{}
			}

			for _, a := range args {
				if a == obj.MemberId {
					continue Outer
				}
			}

			args = append(args, obj.MemberId)

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`member`),
		qm.WhereIn(`member.id in ?`, args...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Member")
	}

	var resultSlice []*Member
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Member")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for member")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for member")
	}

	if len(memberAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.MemberIdMember = foreign
		if foreign.R == nil {
			foreign.R = &memberR{}
		}
		foreign.R.MemberIdKeepLists = append(foreign.R.MemberIdKeepLists, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.MemberId == foreign.ID {
				local.R.MemberIdMember = foreign
				if foreign.R == nil {
					foreign.R = &memberR{}
				}
				foreign.R.MemberIdKeepLists = append(foreign.R.MemberIdKeepLists, local)
				break
			}
		}
	}

	return nil
}

// LoadKeepIdKeepSongs allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (keepListL) LoadKeepIdKeepSongs(ctx context.Context, e boil.ContextExecutor, singular bool, maybeKeepList interface{}, mods queries.Applicator) error {
	var slice []*KeepList
	var object *KeepList

	if singular {
		var ok bool
		object, ok = maybeKeepList.(*KeepList)
		if !ok {
			object = new(KeepList)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeKeepList)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeKeepList))
			}
		}
	} else {
		s, ok := maybeKeepList.(*[]*KeepList)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeKeepList)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeKeepList))
			}
		}
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &keepListR{}
		}
		args = append(args, object.KeepId)
	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &keepListR{}
			}

			for _, a := range args {
				if a == obj.KeepId {
					continue Outer
				}
			}

			args = append(args, obj.KeepId)
		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`keepSong`),
		qm.WhereIn(`keepSong.keepId in ?`, args...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load keepSong")
	}

	var resultSlice []*KeepSong
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice keepSong")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on keepSong")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for keepSong")
	}

	if len(keepSongAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.KeepIdKeepSongs = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &keepSongR{}
			}
			foreign.R.KeepIdKeepList = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.KeepId == foreign.KeepId {
				local.R.KeepIdKeepSongs = append(local.R.KeepIdKeepSongs, foreign)
				if foreign.R == nil {
					foreign.R = &keepSongR{}
				}
				foreign.R.KeepIdKeepList = local
				break
			}
		}
	}

	return nil
}

// SetMemberIdMember of the keepList to the related item.
// Sets o.R.MemberIdMember to related.
// Adds o to related.R.MemberIdKeepLists.
func (o *KeepList) SetMemberIdMember(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Member) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `keepList` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"memberId"}),
		strmangle.WhereClause("`", "`", 0, keepListPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.KeepId}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.MemberId = related.ID
	if o.R == nil {
		o.R = &keepListR{
			MemberIdMember: related,
		}
	} else {
		o.R.MemberIdMember = related
	}

	if related.R == nil {
		related.R = &memberR{
			MemberIdKeepLists: KeepListSlice{o},
		}
	} else {
		related.R.MemberIdKeepLists = append(related.R.MemberIdKeepLists, o)
	}

	return nil
}

// AddKeepIdKeepSongs adds the given related objects to the existing relationships
// of the keepList, optionally inserting them as new records.
// Appends related to o.R.KeepIdKeepSongs.
// Sets related.R.KeepIdKeepList appropriately.
func (o *KeepList) AddKeepIdKeepSongs(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*KeepSong) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.KeepId = o.KeepId
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `keepSong` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"keepId"}),
				strmangle.WhereClause("`", "`", 0, keepSongPrimaryKeyColumns),
			)
			values := []interface{}{o.KeepId, rel.KeepSongId}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.KeepId = o.KeepId
		}
	}

	if o.R == nil {
		o.R = &keepListR{
			KeepIdKeepSongs: related,
		}
	} else {
		o.R.KeepIdKeepSongs = append(o.R.KeepIdKeepSongs, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &keepSongR{
				KeepIdKeepList: o,
			}
		} else {
			rel.R.KeepIdKeepList = o
		}
	}
	return nil
}

// KeepLists retrieves all the records using an executor.
func KeepLists(mods ...qm.QueryMod) keepListQuery {
	mods = append(mods, qm.From("`keepList`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`keepList`.*"})
	}

	return keepListQuery{q}
}

// FindKeepList retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindKeepList(ctx context.Context, exec boil.ContextExecutor, keepId int64, selectCols ...string) (*KeepList, error) {
	keepListObj := &KeepList{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `keepList` where `keepId`=?", sel,
	)

	q := queries.Raw(query, keepId)

	err := q.Bind(ctx, exec, keepListObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "mysql: unable to select from keepList")
	}

	if err = keepListObj.doAfterSelectHooks(ctx, exec); err != nil {
		return keepListObj, err
	}

	return keepListObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *KeepList) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("mysql: no keepList provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(keepListColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	keepListInsertCacheMut.RLock()
	cache, cached := keepListInsertCache[key]
	keepListInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			keepListAllColumns,
			keepListColumnsWithDefault,
			keepListColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(keepListType, keepListMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(keepListType, keepListMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `keepList` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `keepList` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `keepList` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, keepListPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "mysql: unable to insert into keepList")
	}

	var lastID int64
	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.KeepId = int64(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == keepListMapping["keepId"] {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.KeepId,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "mysql: unable to populate default values for keepList")
	}

CacheNoHooks:
	if !cached {
		keepListInsertCacheMut.Lock()
		keepListInsertCache[key] = cache
		keepListInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the KeepList.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *KeepList) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	keepListUpdateCacheMut.RLock()
	cache, cached := keepListUpdateCache[key]
	keepListUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			keepListAllColumns,
			keepListPrimaryKeyColumns,
		)
		if len(wl) == 0 {
			return 0, errors.New("mysql: unable to update keepList, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `keepList` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, keepListPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(keepListType, keepListMapping, append(wl, keepListPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "mysql: unable to update keepList row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "mysql: failed to get rows affected by update for keepList")
	}

	if !cached {
		keepListUpdateCacheMut.Lock()
		keepListUpdateCache[key] = cache
		keepListUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q keepListQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "mysql: unable to update all for keepList")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "mysql: unable to retrieve rows affected for keepList")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o KeepListSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("mysql: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), keepListPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `keepList` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, keepListPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "mysql: unable to update all in keepList slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "mysql: unable to retrieve rows affected all in update all keepList")
	}
	return rowsAff, nil
}

var mySQLKeepListUniqueColumns = []string{
	"keepId",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *KeepList) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("mysql: no keepList provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(keepListColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLKeepListUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	keepListUpsertCacheMut.RLock()
	cache, cached := keepListUpsertCache[key]
	keepListUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			keepListAllColumns,
			keepListColumnsWithDefault,
			keepListColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			keepListAllColumns,
			keepListPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("mysql: unable to upsert keepList, could not build update column list")
		}

		ret = strmangle.SetComplement(ret, nzUniques)
		cache.query = buildUpsertQueryMySQL(dialect, "`keepList`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `keepList` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(keepListType, keepListMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(keepListType, keepListMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "mysql: unable to upsert for keepList")
	}

	var lastID int64
	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.KeepId = int64(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == keepListMapping["keepId"] {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(keepListType, keepListMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "mysql: unable to retrieve unique values for keepList")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "mysql: unable to populate default values for keepList")
	}

CacheNoHooks:
	if !cached {
		keepListUpsertCacheMut.Lock()
		keepListUpsertCache[key] = cache
		keepListUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single KeepList record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *KeepList) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("mysql: no KeepList provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), keepListPrimaryKeyMapping)
	sql := "DELETE FROM `keepList` WHERE `keepId`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "mysql: unable to delete from keepList")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "mysql: failed to get rows affected by delete for keepList")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q keepListQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("mysql: no keepListQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "mysql: unable to delete all from keepList")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "mysql: failed to get rows affected by deleteall for keepList")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o KeepListSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(keepListBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), keepListPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `keepList` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, keepListPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "mysql: unable to delete all from keepList slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "mysql: failed to get rows affected by deleteall for keepList")
	}

	if len(keepListAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *KeepList) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindKeepList(ctx, exec, o.KeepId)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *KeepListSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := KeepListSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), keepListPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `keepList`.* FROM `keepList` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, keepListPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "mysql: unable to reload all in KeepListSlice")
	}

	*o = slice

	return nil
}

// KeepListExists checks if the KeepList row exists.
func KeepListExists(ctx context.Context, exec boil.ContextExecutor, keepId int64) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `keepList` where `keepId`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, keepId)
	}
	row := exec.QueryRowContext(ctx, sql, keepId)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "mysql: unable to check if keepList exists")
	}

	return exists, nil
}

// Exists checks if the KeepList row exists.
func (o *KeepList) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return KeepListExists(ctx, exec, o.KeepId)
}
