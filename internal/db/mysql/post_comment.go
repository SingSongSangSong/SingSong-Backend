// Code generated by SQLBoiler 4.15.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package mysql

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// PostComment is an object representing the database table.
type PostComment struct {
	PostCommentID       int64       `boil:"post_comment_id" json:"post_comment_id" toml:"post_comment_id" yaml:"post_comment_id"`
	PostID              int64       `boil:"post_id" json:"post_id" toml:"post_id" yaml:"post_id"`
	MemberID            int64       `boil:"member_id" json:"member_id" toml:"member_id" yaml:"member_id"`
	Content             null.String `boil:"content" json:"content,omitempty" toml:"content" yaml:"content,omitempty"`
	Likes               int         `boil:"likes" json:"likes" toml:"likes" yaml:"likes"`
	IsRecomment         null.Bool   `boil:"is_recomment" json:"is_recomment,omitempty" toml:"is_recomment" yaml:"is_recomment,omitempty"`
	ParentPostCommentID null.Int64  `boil:"parent_post_comment_id" json:"parent_post_comment_id,omitempty" toml:"parent_post_comment_id" yaml:"parent_post_comment_id,omitempty"`
	CreatedAt           null.Time   `boil:"created_at" json:"created_at,omitempty" toml:"created_at" yaml:"created_at,omitempty"`
	UpdatedAt           null.Time   `boil:"updated_at" json:"updated_at,omitempty" toml:"updated_at" yaml:"updated_at,omitempty"`
	DeletedAt           null.Time   `boil:"deleted_at" json:"deleted_at,omitempty" toml:"deleted_at" yaml:"deleted_at,omitempty"`

	R *postCommentR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L postCommentL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var PostCommentColumns = struct {
	PostCommentID       string
	PostID              string
	MemberID            string
	Content             string
	Likes               string
	IsRecomment         string
	ParentPostCommentID string
	CreatedAt           string
	UpdatedAt           string
	DeletedAt           string
}{
	PostCommentID:       "post_comment_id",
	PostID:              "post_id",
	MemberID:            "member_id",
	Content:             "content",
	Likes:               "likes",
	IsRecomment:         "is_recomment",
	ParentPostCommentID: "parent_post_comment_id",
	CreatedAt:           "created_at",
	UpdatedAt:           "updated_at",
	DeletedAt:           "deleted_at",
}

var PostCommentTableColumns = struct {
	PostCommentID       string
	PostID              string
	MemberID            string
	Content             string
	Likes               string
	IsRecomment         string
	ParentPostCommentID string
	CreatedAt           string
	UpdatedAt           string
	DeletedAt           string
}{
	PostCommentID:       "post_comment.post_comment_id",
	PostID:              "post_comment.post_id",
	MemberID:            "post_comment.member_id",
	Content:             "post_comment.content",
	Likes:               "post_comment.likes",
	IsRecomment:         "post_comment.is_recomment",
	ParentPostCommentID: "post_comment.parent_post_comment_id",
	CreatedAt:           "post_comment.created_at",
	UpdatedAt:           "post_comment.updated_at",
	DeletedAt:           "post_comment.deleted_at",
}

// Generated where

var PostCommentWhere = struct {
	PostCommentID       whereHelperint64
	PostID              whereHelperint64
	MemberID            whereHelperint64
	Content             whereHelpernull_String
	Likes               whereHelperint
	IsRecomment         whereHelpernull_Bool
	ParentPostCommentID whereHelpernull_Int64
	CreatedAt           whereHelpernull_Time
	UpdatedAt           whereHelpernull_Time
	DeletedAt           whereHelpernull_Time
}{
	PostCommentID:       whereHelperint64{field: "`post_comment`.`post_comment_id`"},
	PostID:              whereHelperint64{field: "`post_comment`.`post_id`"},
	MemberID:            whereHelperint64{field: "`post_comment`.`member_id`"},
	Content:             whereHelpernull_String{field: "`post_comment`.`content`"},
	Likes:               whereHelperint{field: "`post_comment`.`likes`"},
	IsRecomment:         whereHelpernull_Bool{field: "`post_comment`.`is_recomment`"},
	ParentPostCommentID: whereHelpernull_Int64{field: "`post_comment`.`parent_post_comment_id`"},
	CreatedAt:           whereHelpernull_Time{field: "`post_comment`.`created_at`"},
	UpdatedAt:           whereHelpernull_Time{field: "`post_comment`.`updated_at`"},
	DeletedAt:           whereHelpernull_Time{field: "`post_comment`.`deleted_at`"},
}

// PostCommentRels is where relationship names are stored.
var PostCommentRels = struct {
	Member           string
	Post             string
	PostCommentSongs string
}{
	Member:           "Member",
	Post:             "Post",
	PostCommentSongs: "PostCommentSongs",
}

// postCommentR is where relationships are stored.
type postCommentR struct {
	Member           *Member              `boil:"Member" json:"Member" toml:"Member" yaml:"Member"`
	Post             *Post                `boil:"Post" json:"Post" toml:"Post" yaml:"Post"`
	PostCommentSongs PostCommentSongSlice `boil:"PostCommentSongs" json:"PostCommentSongs" toml:"PostCommentSongs" yaml:"PostCommentSongs"`
}

// NewStruct creates a new relationship struct
func (*postCommentR) NewStruct() *postCommentR {
	return &postCommentR{}
}

func (r *postCommentR) GetMember() *Member {
	if r == nil {
		return nil
	}
	return r.Member
}

func (r *postCommentR) GetPost() *Post {
	if r == nil {
		return nil
	}
	return r.Post
}

func (r *postCommentR) GetPostCommentSongs() PostCommentSongSlice {
	if r == nil {
		return nil
	}
	return r.PostCommentSongs
}

// postCommentL is where Load methods for each relationship are stored.
type postCommentL struct{}

var (
	postCommentAllColumns            = []string{"post_comment_id", "post_id", "member_id", "content", "likes", "is_recomment", "parent_post_comment_id", "created_at", "updated_at", "deleted_at"}
	postCommentColumnsWithoutDefault = []string{"post_id", "member_id", "content", "likes", "parent_post_comment_id", "deleted_at"}
	postCommentColumnsWithDefault    = []string{"post_comment_id", "is_recomment", "created_at", "updated_at"}
	postCommentPrimaryKeyColumns     = []string{"post_comment_id"}
	postCommentGeneratedColumns      = []string{}
)

type (
	// PostCommentSlice is an alias for a slice of pointers to PostComment.
	// This should almost always be used instead of []PostComment.
	PostCommentSlice []*PostComment
	// PostCommentHook is the signature for custom PostComment hook methods
	PostCommentHook func(context.Context, boil.ContextExecutor, *PostComment) error

	postCommentQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	postCommentType                 = reflect.TypeOf(&PostComment{})
	postCommentMapping              = queries.MakeStructMapping(postCommentType)
	postCommentPrimaryKeyMapping, _ = queries.BindMapping(postCommentType, postCommentMapping, postCommentPrimaryKeyColumns)
	postCommentInsertCacheMut       sync.RWMutex
	postCommentInsertCache          = make(map[string]insertCache)
	postCommentUpdateCacheMut       sync.RWMutex
	postCommentUpdateCache          = make(map[string]updateCache)
	postCommentUpsertCacheMut       sync.RWMutex
	postCommentUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var postCommentAfterSelectHooks []PostCommentHook

var postCommentBeforeInsertHooks []PostCommentHook
var postCommentAfterInsertHooks []PostCommentHook

var postCommentBeforeUpdateHooks []PostCommentHook
var postCommentAfterUpdateHooks []PostCommentHook

var postCommentBeforeDeleteHooks []PostCommentHook
var postCommentAfterDeleteHooks []PostCommentHook

var postCommentBeforeUpsertHooks []PostCommentHook
var postCommentAfterUpsertHooks []PostCommentHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *PostComment) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range postCommentAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *PostComment) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range postCommentBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *PostComment) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range postCommentAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *PostComment) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range postCommentBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *PostComment) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range postCommentAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *PostComment) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range postCommentBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *PostComment) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range postCommentAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *PostComment) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range postCommentBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *PostComment) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range postCommentAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddPostCommentHook registers your hook function for all future operations.
func AddPostCommentHook(hookPoint boil.HookPoint, postCommentHook PostCommentHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		postCommentAfterSelectHooks = append(postCommentAfterSelectHooks, postCommentHook)
	case boil.BeforeInsertHook:
		postCommentBeforeInsertHooks = append(postCommentBeforeInsertHooks, postCommentHook)
	case boil.AfterInsertHook:
		postCommentAfterInsertHooks = append(postCommentAfterInsertHooks, postCommentHook)
	case boil.BeforeUpdateHook:
		postCommentBeforeUpdateHooks = append(postCommentBeforeUpdateHooks, postCommentHook)
	case boil.AfterUpdateHook:
		postCommentAfterUpdateHooks = append(postCommentAfterUpdateHooks, postCommentHook)
	case boil.BeforeDeleteHook:
		postCommentBeforeDeleteHooks = append(postCommentBeforeDeleteHooks, postCommentHook)
	case boil.AfterDeleteHook:
		postCommentAfterDeleteHooks = append(postCommentAfterDeleteHooks, postCommentHook)
	case boil.BeforeUpsertHook:
		postCommentBeforeUpsertHooks = append(postCommentBeforeUpsertHooks, postCommentHook)
	case boil.AfterUpsertHook:
		postCommentAfterUpsertHooks = append(postCommentAfterUpsertHooks, postCommentHook)
	}
}

// One returns a single postComment record from the query.
func (q postCommentQuery) One(ctx context.Context, exec boil.ContextExecutor) (*PostComment, error) {
	o := &PostComment{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "mysql: failed to execute a one query for post_comment")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all PostComment records from the query.
func (q postCommentQuery) All(ctx context.Context, exec boil.ContextExecutor) (PostCommentSlice, error) {
	var o []*PostComment

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "mysql: failed to assign all query results to PostComment slice")
	}

	if len(postCommentAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all PostComment records in the query.
func (q postCommentQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "mysql: failed to count post_comment rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q postCommentQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "mysql: failed to check if post_comment exists")
	}

	return count > 0, nil
}

// Member pointed to by the foreign key.
func (o *PostComment) Member(mods ...qm.QueryMod) memberQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`member_id` = ?", o.MemberID),
	}

	queryMods = append(queryMods, mods...)

	return Members(queryMods...)
}

// Post pointed to by the foreign key.
func (o *PostComment) Post(mods ...qm.QueryMod) postQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`post_id` = ?", o.PostID),
	}

	queryMods = append(queryMods, mods...)

	return Posts(queryMods...)
}

// PostCommentSongs retrieves all the post_comment_song's PostCommentSongs with an executor.
func (o *PostComment) PostCommentSongs(mods ...qm.QueryMod) postCommentSongQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`post_comment_song`.`post_comment_id`=?", o.PostCommentID),
	)

	return PostCommentSongs(queryMods...)
}

// LoadMember allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (postCommentL) LoadMember(ctx context.Context, e boil.ContextExecutor, singular bool, maybePostComment interface{}, mods queries.Applicator) error {
	var slice []*PostComment
	var object *PostComment

	if singular {
		var ok bool
		object, ok = maybePostComment.(*PostComment)
		if !ok {
			object = new(PostComment)
			ok = queries.SetFromEmbeddedStruct(&object, &maybePostComment)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybePostComment))
			}
		}
	} else {
		s, ok := maybePostComment.(*[]*PostComment)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybePostComment)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybePostComment))
			}
		}
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &postCommentR{}
		}
		args = append(args, object.MemberID)

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &postCommentR{}
			}

			for _, a := range args {
				if a == obj.MemberID {
					continue Outer
				}
			}

			args = append(args, obj.MemberID)

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`member`),
		qm.WhereIn(`member.member_id in ?`, args...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Member")
	}

	var resultSlice []*Member
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Member")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for member")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for member")
	}

	if len(memberAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Member = foreign
		if foreign.R == nil {
			foreign.R = &memberR{}
		}
		foreign.R.PostComments = append(foreign.R.PostComments, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.MemberID == foreign.MemberID {
				local.R.Member = foreign
				if foreign.R == nil {
					foreign.R = &memberR{}
				}
				foreign.R.PostComments = append(foreign.R.PostComments, local)
				break
			}
		}
	}

	return nil
}

// LoadPost allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (postCommentL) LoadPost(ctx context.Context, e boil.ContextExecutor, singular bool, maybePostComment interface{}, mods queries.Applicator) error {
	var slice []*PostComment
	var object *PostComment

	if singular {
		var ok bool
		object, ok = maybePostComment.(*PostComment)
		if !ok {
			object = new(PostComment)
			ok = queries.SetFromEmbeddedStruct(&object, &maybePostComment)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybePostComment))
			}
		}
	} else {
		s, ok := maybePostComment.(*[]*PostComment)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybePostComment)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybePostComment))
			}
		}
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &postCommentR{}
		}
		args = append(args, object.PostID)

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &postCommentR{}
			}

			for _, a := range args {
				if a == obj.PostID {
					continue Outer
				}
			}

			args = append(args, obj.PostID)

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`post`),
		qm.WhereIn(`post.post_id in ?`, args...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Post")
	}

	var resultSlice []*Post
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Post")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for post")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for post")
	}

	if len(postAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Post = foreign
		if foreign.R == nil {
			foreign.R = &postR{}
		}
		foreign.R.PostComments = append(foreign.R.PostComments, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.PostID == foreign.PostID {
				local.R.Post = foreign
				if foreign.R == nil {
					foreign.R = &postR{}
				}
				foreign.R.PostComments = append(foreign.R.PostComments, local)
				break
			}
		}
	}

	return nil
}

// LoadPostCommentSongs allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (postCommentL) LoadPostCommentSongs(ctx context.Context, e boil.ContextExecutor, singular bool, maybePostComment interface{}, mods queries.Applicator) error {
	var slice []*PostComment
	var object *PostComment

	if singular {
		var ok bool
		object, ok = maybePostComment.(*PostComment)
		if !ok {
			object = new(PostComment)
			ok = queries.SetFromEmbeddedStruct(&object, &maybePostComment)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybePostComment))
			}
		}
	} else {
		s, ok := maybePostComment.(*[]*PostComment)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybePostComment)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybePostComment))
			}
		}
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &postCommentR{}
		}
		args = append(args, object.PostCommentID)
	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &postCommentR{}
			}

			for _, a := range args {
				if a == obj.PostCommentID {
					continue Outer
				}
			}

			args = append(args, obj.PostCommentID)
		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`post_comment_song`),
		qm.WhereIn(`post_comment_song.post_comment_id in ?`, args...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load post_comment_song")
	}

	var resultSlice []*PostCommentSong
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice post_comment_song")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on post_comment_song")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for post_comment_song")
	}

	if len(postCommentSongAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.PostCommentSongs = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &postCommentSongR{}
			}
			foreign.R.PostComment = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.PostCommentID == foreign.PostCommentID {
				local.R.PostCommentSongs = append(local.R.PostCommentSongs, foreign)
				if foreign.R == nil {
					foreign.R = &postCommentSongR{}
				}
				foreign.R.PostComment = local
				break
			}
		}
	}

	return nil
}

// SetMember of the postComment to the related item.
// Sets o.R.Member to related.
// Adds o to related.R.PostComments.
func (o *PostComment) SetMember(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Member) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `post_comment` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"member_id"}),
		strmangle.WhereClause("`", "`", 0, postCommentPrimaryKeyColumns),
	)
	values := []interface{}{related.MemberID, o.PostCommentID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.MemberID = related.MemberID
	if o.R == nil {
		o.R = &postCommentR{
			Member: related,
		}
	} else {
		o.R.Member = related
	}

	if related.R == nil {
		related.R = &memberR{
			PostComments: PostCommentSlice{o},
		}
	} else {
		related.R.PostComments = append(related.R.PostComments, o)
	}

	return nil
}

// SetPost of the postComment to the related item.
// Sets o.R.Post to related.
// Adds o to related.R.PostComments.
func (o *PostComment) SetPost(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Post) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `post_comment` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"post_id"}),
		strmangle.WhereClause("`", "`", 0, postCommentPrimaryKeyColumns),
	)
	values := []interface{}{related.PostID, o.PostCommentID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.PostID = related.PostID
	if o.R == nil {
		o.R = &postCommentR{
			Post: related,
		}
	} else {
		o.R.Post = related
	}

	if related.R == nil {
		related.R = &postR{
			PostComments: PostCommentSlice{o},
		}
	} else {
		related.R.PostComments = append(related.R.PostComments, o)
	}

	return nil
}

// AddPostCommentSongs adds the given related objects to the existing relationships
// of the post_comment, optionally inserting them as new records.
// Appends related to o.R.PostCommentSongs.
// Sets related.R.PostComment appropriately.
func (o *PostComment) AddPostCommentSongs(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*PostCommentSong) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.PostCommentID = o.PostCommentID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `post_comment_song` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"post_comment_id"}),
				strmangle.WhereClause("`", "`", 0, postCommentSongPrimaryKeyColumns),
			)
			values := []interface{}{o.PostCommentID, rel.PostCommentSongID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.PostCommentID = o.PostCommentID
		}
	}

	if o.R == nil {
		o.R = &postCommentR{
			PostCommentSongs: related,
		}
	} else {
		o.R.PostCommentSongs = append(o.R.PostCommentSongs, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &postCommentSongR{
				PostComment: o,
			}
		} else {
			rel.R.PostComment = o
		}
	}
	return nil
}

// PostComments retrieves all the records using an executor.
func PostComments(mods ...qm.QueryMod) postCommentQuery {
	mods = append(mods, qm.From("`post_comment`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`post_comment`.*"})
	}

	return postCommentQuery{q}
}

// FindPostComment retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindPostComment(ctx context.Context, exec boil.ContextExecutor, postCommentID int64, selectCols ...string) (*PostComment, error) {
	postCommentObj := &PostComment{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `post_comment` where `post_comment_id`=?", sel,
	)

	q := queries.Raw(query, postCommentID)

	err := q.Bind(ctx, exec, postCommentObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "mysql: unable to select from post_comment")
	}

	if err = postCommentObj.doAfterSelectHooks(ctx, exec); err != nil {
		return postCommentObj, err
	}

	return postCommentObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *PostComment) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("mysql: no post_comment provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(postCommentColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	postCommentInsertCacheMut.RLock()
	cache, cached := postCommentInsertCache[key]
	postCommentInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			postCommentAllColumns,
			postCommentColumnsWithDefault,
			postCommentColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(postCommentType, postCommentMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(postCommentType, postCommentMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `post_comment` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `post_comment` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `post_comment` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, postCommentPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "mysql: unable to insert into post_comment")
	}

	var lastID int64
	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.PostCommentID = int64(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == postCommentMapping["post_comment_id"] {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.PostCommentID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "mysql: unable to populate default values for post_comment")
	}

CacheNoHooks:
	if !cached {
		postCommentInsertCacheMut.Lock()
		postCommentInsertCache[key] = cache
		postCommentInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the PostComment.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *PostComment) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	postCommentUpdateCacheMut.RLock()
	cache, cached := postCommentUpdateCache[key]
	postCommentUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			postCommentAllColumns,
			postCommentPrimaryKeyColumns,
		)
		if len(wl) == 0 {
			return 0, errors.New("mysql: unable to update post_comment, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `post_comment` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, postCommentPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(postCommentType, postCommentMapping, append(wl, postCommentPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "mysql: unable to update post_comment row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "mysql: failed to get rows affected by update for post_comment")
	}

	if !cached {
		postCommentUpdateCacheMut.Lock()
		postCommentUpdateCache[key] = cache
		postCommentUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q postCommentQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "mysql: unable to update all for post_comment")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "mysql: unable to retrieve rows affected for post_comment")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o PostCommentSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("mysql: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), postCommentPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `post_comment` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, postCommentPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "mysql: unable to update all in postComment slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "mysql: unable to retrieve rows affected all in update all postComment")
	}
	return rowsAff, nil
}

var mySQLPostCommentUniqueColumns = []string{
	"post_comment_id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *PostComment) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("mysql: no post_comment provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(postCommentColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLPostCommentUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	postCommentUpsertCacheMut.RLock()
	cache, cached := postCommentUpsertCache[key]
	postCommentUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			postCommentAllColumns,
			postCommentColumnsWithDefault,
			postCommentColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			postCommentAllColumns,
			postCommentPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("mysql: unable to upsert post_comment, could not build update column list")
		}

		ret = strmangle.SetComplement(ret, nzUniques)
		cache.query = buildUpsertQueryMySQL(dialect, "`post_comment`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `post_comment` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(postCommentType, postCommentMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(postCommentType, postCommentMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "mysql: unable to upsert for post_comment")
	}

	var lastID int64
	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.PostCommentID = int64(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == postCommentMapping["post_comment_id"] {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(postCommentType, postCommentMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "mysql: unable to retrieve unique values for post_comment")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "mysql: unable to populate default values for post_comment")
	}

CacheNoHooks:
	if !cached {
		postCommentUpsertCacheMut.Lock()
		postCommentUpsertCache[key] = cache
		postCommentUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single PostComment record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *PostComment) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("mysql: no PostComment provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), postCommentPrimaryKeyMapping)
	sql := "DELETE FROM `post_comment` WHERE `post_comment_id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "mysql: unable to delete from post_comment")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "mysql: failed to get rows affected by delete for post_comment")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q postCommentQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("mysql: no postCommentQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "mysql: unable to delete all from post_comment")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "mysql: failed to get rows affected by deleteall for post_comment")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o PostCommentSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(postCommentBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), postCommentPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `post_comment` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, postCommentPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "mysql: unable to delete all from postComment slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "mysql: failed to get rows affected by deleteall for post_comment")
	}

	if len(postCommentAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *PostComment) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindPostComment(ctx, exec, o.PostCommentID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *PostCommentSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := PostCommentSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), postCommentPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `post_comment`.* FROM `post_comment` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, postCommentPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "mysql: unable to reload all in PostCommentSlice")
	}

	*o = slice

	return nil
}

// PostCommentExists checks if the PostComment row exists.
func PostCommentExists(ctx context.Context, exec boil.ContextExecutor, postCommentID int64) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `post_comment` where `post_comment_id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, postCommentID)
	}
	row := exec.QueryRowContext(ctx, sql, postCommentID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "mysql: unable to check if post_comment exists")
	}

	return exists, nil
}

// Exists checks if the PostComment row exists.
func (o *PostComment) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return PostCommentExists(ctx, exec, o.PostCommentID)
}
